apply plugin: 'com.android.application'

android {
    signingConfigs {
        spider {
            keyAlias 'spider'
            keyPassword 'muzka113162'
            storeFile file('C:/Users/User/AndroidStudioProjects/spider.jks')
            storePassword 'muzka113162'
        }
    }
    compileSdkVersion 17
    buildToolsVersion "22.0.1"
    defaultConfig {
        applicationId "vc.spider.consult"
        minSdkVersion 17
        targetSdkVersion 17
        versionCode 1000
        versionName "1.0"
    }
    buildTypes {
        release {
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.spider
        }
    }
    productFlavors {
    }
}

dependencies {
    compile project(':library:actionbarsherlock')
    compile project(':library:RoundCornerProgressBar')
    compile project(':library:usbserialforandroid')
}
